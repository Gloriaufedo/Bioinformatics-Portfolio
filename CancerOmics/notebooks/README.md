# Notebooks

This directory contains exploratory Jupyter notebooks used during development.

Only notebooks that demonstrate analyses or validate methods will be stored here. Final reusable code is implemented in the `src/` directory.
